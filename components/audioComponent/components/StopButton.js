import React from 'react'
import { View } from 'react-native'
import * as Icon from '@expo/vector-icons'

const StopButton = () => {
    return (
        <Icon.FontAwesome name='stop' size={20} color='gray' />
    )
}

export default StopButton